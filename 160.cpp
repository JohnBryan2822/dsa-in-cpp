// You have been given a text of no more than 500 words.
// Your task is to convert lowercase letters to uppercase and uppercase letters in this text.

#include <bits/stdc++.h>
using namespace std;
int main()
{
    double x, y;
    cin>>x>>y;
    cout<<max(x, y)<<" "<<min(x, y);
}
